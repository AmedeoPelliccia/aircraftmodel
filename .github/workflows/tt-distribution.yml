<<<<<<< HEAD
name: TT Distribution Check
=======
name: TT Distribution
>>>>>>> origin/main

on:
  pull_request:
    types: [closed]

permissions:
  contents: read

jobs:
<<<<<<< HEAD
  verify:
=======
  distribute:
>>>>>>> origin/main
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Check for KNU ID in PR
        id: knu_check
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          BODY="${{ github.event.pull_request.body }}"
          if echo "$TITLE $BODY" | grep -qE 'KNU-ATA[0-9]{2}-[0-9]{2}-[0-9]{3}'; then
            echo "found=true" >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Verify TT ledger
        if: steps.knu_check.outputs.found == 'true'
=======

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check for KNU ID in PR
        id: knu-check
        run: |
          PR_BODY="${{ github.event.pull_request.body }}"
          PR_TITLE="${{ github.event.pull_request.title }}"
          if echo "$PR_TITLE $PR_BODY" | grep -qE 'KNU-[A-Z0-9-]+'; then
            echo "knu_found=true" >> "$GITHUB_OUTPUT"
          else
            echo "knu_found=false" >> "$GITHUB_OUTPUT"
            echo "No KNU ID found in PR — skipping TT distribution"
          fi

      - name: Verify ledger integrity
        if: steps.knu-check.outputs.knu_found == 'true'
>>>>>>> origin/main
        run: python tools/knu_distribution.py verify
